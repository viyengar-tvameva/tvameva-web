---
# infrastructure/gke/namespace.yaml
apiVersion: v1
kind: Namespace
metadata:
  name: drupal
  labels:
    app: tvameva-cms

---
# infrastructure/gke/drupal-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: drupal
  namespace: drupal
  labels:
    app: drupal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: drupal
  template:
    metadata:
      labels:
        app: drupal
    spec:
      containers:
        - name: drupal
          image: drupal:10-apache
          ports:
            - containerPort: 80
              name: http
          env:
            - name: DRUPAL_DB_HOST
              value: "127.0.0.1"  # Cloud SQL Proxy sidecar
            - name: DRUPAL_DB_PORT
              value: "3306"
            - name: DRUPAL_DB_NAME
              value: "drupal"
            - name: DRUPAL_DB_USER
              value: "drupal"
            - name: DRUPAL_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: drupal-secrets
                  key: db-password
          volumeMounts:
            - name: drupal-files
              mountPath: /var/www/html/sites/default/files
            - name: drupal-modules
              mountPath: /var/www/html/modules/custom
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 60
            periodSeconds: 30

        # Cloud SQL Proxy sidecar
        - name: cloud-sql-proxy
          image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2
          args:
            - "--structured-logs"
            - "--auto-iam-authn"
            # Replace with your Cloud SQL connection name
            - "PROJECT_ID:REGION:tvameva-drupal-db"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"

      volumes:
        - name: drupal-files
          persistentVolumeClaim:
            claimName: drupal-files-pvc
        - name: drupal-modules
          persistentVolumeClaim:
            claimName: drupal-modules-pvc

---
# infrastructure/gke/drupal-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: drupal-service
  namespace: drupal
  labels:
    app: drupal
spec:
  type: ClusterIP
  selector:
    app: drupal
  ports:
    - port: 8080
      targetPort: 80
      protocol: TCP
      name: http

---
# infrastructure/gke/drupal-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: drupal-files-pvc
  namespace: drupal
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard-rwo

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: drupal-modules-pvc
  namespace: drupal
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard-rwo

---
# infrastructure/gke/drupal-ingress.yaml
# Exposes Drupal CMS admin at cms.tvameva.ai
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: drupal-ingress
  namespace: drupal
  annotations:
    kubernetes.io/ingress.class: "gce"
    kubernetes.io/ingress.global-static-ip-name: "tvameva-cms-ip"
    networking.gke.io/managed-certificates: "tvameva-cms-cert"
spec:
  rules:
    - host: cms.tvameva.ai
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: drupal-service
                port:
                  number: 8080

---
# infrastructure/gke/managed-cert.yaml
apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: tvameva-cms-cert
  namespace: drupal
spec:
  domains:
    - cms.tvameva.ai
